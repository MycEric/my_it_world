spring:
  application:
    name: blog-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER:10.8.7.58:8848}
      config:
        server-addr: ${NACOS_SERVER:10.8.7.58:8848}
        file-extension: yaml
